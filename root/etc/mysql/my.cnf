[client]
port=@MYSQL_PORT@
socket=@MYSQL_SOCKET@

[mysql]
port=@MYSQL_PORT@
socket=@MYSQL_SOCKET@

[mysqldump]
datadir=@MYSQL_DATA_DIR@
port=@MYSQL_PORT@
socket=@MYSQL_SOCKET@

[mysqld_safe]
bind-address=127.0.0.1
datadir=@MYSQL_DATA_DIR@
port=@MYSQL_PORT@
socket=@MYSQL_SOCKET@

[mysqld]
bind-address=127.0.0.1
datadir=@MYSQL_DATA_DIR@
port=@MYSQL_PORT@
socket=@MYSQL_SOCKET@

[mysqladmin]
port=@MYSQL_PORT@
socket=@MYSQL_SOCKET@

[mysql_secure_installation]
port=@MYSQL_PORT@
socket=@MYSQL_SOCKET@
